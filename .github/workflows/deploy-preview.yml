name: Netlify Preview Deploy

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Note
        run: echo "Netlify auto-deploys previews via its GitHub integration. No extra config needed here once sites are connected."
